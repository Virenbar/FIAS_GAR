<Window x:Class="MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FIAS_Updater"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <DockPanel>
        <Menu DockPanel.Dock="Top">
            <MenuItem Name="MI_ReadFI" Header="Read FI" />
            <MenuItem Name="MI_Decompress" Header="Decompress FI" />
        </Menu>
        <StatusBar  VerticalAlignment="Bottom" DockPanel.Dock="Bottom">
            <StatusBarItem>
                <TextBlock Text="t" />
            </StatusBarItem>
        </StatusBar>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="1*" />
                <RowDefinition Height="1*" />
                <RowDefinition Height="auto" />
                <RowDefinition Height="auto" />
            </Grid.RowDefinitions>

            <ListView Grid.Row="0" Margin="5" />
            <ListView Grid.Row="1" Margin="5" />
            <StackPanel Grid.Row="2">
                <Button Name="B_GetFiles" Content="Получить список" HorizontalAlignment="Left" Margin="5" VerticalAlignment="Top" Height="22" Width="100" />
            </StackPanel>
        </Grid>
    </DockPanel>
</Window>